1. add following columns to the gateDevices table
  e_boardCOM_ID, gatebarCOM_ID

datatype: tinyint
comment: 1, 2, ... 8
default: 3
not null: true

2. 읽어서 전역변수(deviceComID)에 위 값을 넣는다

3. 저장할 때 CBox 에서 값을 채취하여 그 값(레이블)을 저장한다.
    제 3175 행 부근

4. 연결할 때 전역변수의 값을 사용하여 'COM?' 문자열을 생성한다.




